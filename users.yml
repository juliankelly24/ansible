---
  users:
  
  - username: julian
    use_sudo: yes
    key: "{{ lookup('file', '/opt/aaf/repo/pub_keys/julian.pub') }}"
    
  - username: dalia
    use_sudo: yes
 
  - username: terry
    use_sudo: yes
 
  - username: melroy
    use_sudo: yes

  - username: ec2-user
    use_sudo: yes
    key: "{{ lookup('file', '/home/ec2-user/.ssh/authorized_keys') }}"
